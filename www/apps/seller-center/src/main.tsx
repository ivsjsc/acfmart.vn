import React from 'react'
import ReactDOM from 'react-dom/client'
import SellerCenter from '../App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SellerCenter />
  </React.StrictMode>,
)