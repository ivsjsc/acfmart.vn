// Root build file for the acfmart monorepo
// NOTE: Android projects should be built from their own directories
// Do not run Gradle commands from root for Android builds

plugins {
    // Add plugins here if needed
}

// Only configure root project - do not apply to subprojects
repositories {
    google()
    mavenCentral()
}
